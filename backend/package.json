{
  "name": "relive-backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js",
    "db:generate": "prisma generate",
    "predb:migrate": "node scripts/ensure-database.js",
    "db:migrate": "prisma migrate dev",
    "predb:deploy": "node scripts/ensure-database.js",
    "db:deploy": "prisma migrate deploy",
    "db:studio": "prisma studio",
    "db:seed": "node prisma/seed.js",
    "setup:auth": "node scripts/setup-local-auth.js",
    "test": "node --test"
  },
  "dependencies": {
    "@prisma/client": "6.19.0",
    "cors": "^2.8.5",
    "dotenv": "^16.5.0",
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.2"
  },
  "devDependencies": {
    "prisma": "6.19.0"
  }
}
